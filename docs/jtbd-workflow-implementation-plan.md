# JTBD Workflow Implementation Plan

Static tracker for the workflow implementation program. Source of truth for scope is `docs/platform-jtbd-catalogue.md`.

## Status Legend

| Status | Meaning |
|---|---|
| Not started | No implementation work has begun in this tracker. |
| In progress | Current Ralph slice is actively changing code. |
| Verified | Slice has implementation and fresh targeted verification. |
| Committed | Slice has a local commit. |
| Pushed | Slice commit was pushed to remote. |
| Blocked | External issue prevents completion; blocker recorded. |

## Slice Tracker

| Slice | JTBD IDs | Current support | Backend changes required | Frontend changes required | Data model / migration changes | Workflow / task / notification / audit changes | Tests required | Status | Commit hash | Evidence / gaps |
|---|---|---|---|---|---|---|---|---|---|---|
| P0-A Requirement acceptance and assignment workflow | JTBD-013, JTBD-014, JTBD-019, JTBD-020, JTBD-060 | Requirements can be stored, accepted/rejected/reopened, and accepted requirements project a linked writing task. Broader SLA/notification/escalation remains future work. | Added `transitionRequirementWorkflow` around `rfpRequirements`, `proposalTasks`, and `taskActivity`. | Added workflow controls, gate visibility, session actor use, and history preview to `RequirementDetail`. | No migration; uses existing requirement `metadata` and task/activity tables. | Legal transitions, acceptance gates, Postgres advisory lock, task projection/resync, metadata history with evidence, and task activity audit implemented for pilot workflow. | `npm test -- requirements-unification.test.ts requirements-workflow.test.ts RequirementDetailWorkflow.test.ts`; `npx tsc --noEmit --pretty false`; targeted `npm run lint -- --file ...`. | Pushed | e7842d5 | 11 focused tests pass; TypeScript passes; targeted lint passes. Canonical workflow runtime, universal audit table, notifications, SLA escalation, and full UI rendering tests remain future work. |
| P0-B RFP intake parse lifecycle | JTBD-011, JTBD-012, JTBD-072 | Upload/parse surfaces now expose status polling, governed failed-parse remediation, server-side Linode E3 object storage for manual uploads, and E3 storage for fetched opportunity/RFP documents; security scan, SSE, notifications, and full exception queue remain future work. | Added parse workflow transitions around `rfpDocuments` and `rfpParsingJobs`; parse API retries failed documents through the workflow action; upload route and discovered-document fetch can store RFP binaries in Linode E3 via server-side SigV4 because E3 has no browser CORS. | Updated `RFPUploader` to show parsing progress, terminal parse errors, and parse retry. | No migration; uses existing document/job `metadata` and stores `s3://bucket/key` in existing storage-path fields when Linode E3 env vars are configured. | Retry/reject/manual extraction/cancel states, advisory lock, retry attempt history, terminal success/failure metadata history, and storage receipt paths implemented for pilot workflow. | `npm test -- rfp-parse-workflow.test.ts linode-e3.test.ts rfp-document-service.test.ts`; combined workflow regression set; `npx tsc --noEmit --pretty false`; targeted `npm run lint -- --file ...`; live Linode E3 PUT/GET smoke check through ignored frontend env. | Pushed | 6e01cd4, bdde976, 27727a0 | 10 focused RFP/storage/fetch tests pass; TypeScript passes; targeted lint passes; live upload/download against `mansa` bucket passes. Security scan, SSE/notification/exception dashboard remain future work. |
| P0-C Compliance matrix governance | JTBD-015, JTBD-016, JTBD-017 | Matrix rows now have a governed pilot lifecycle for submit, approve, reject, waive, and reopen with gate checks; final matrix lock, projected gap tasks, notifications, and export/report approval remain future work. | Added `transitionComplianceEntryWorkflow` around `complianceEntries` and `complianceMatrices`, including row locks, transition gates, waiver metadata, and matrix stat recalculation. | Added row workflow actions and reason dialog to `ComplianceMatrix`. | No migration; uses existing review/approval columns and `metadata.complianceWorkflow`. | Legal transitions, required decision reasons, evidence gates for submission/approval, waiver audit metadata, advisory lock, and cached matrix metric refresh implemented for pilot workflow. | `npm test -- --run __tests__/actions/compliance-entry-workflow.test.ts`; `npx tsc --noEmit`. | Pushed | 237d43c | 3 focused compliance workflow tests pass; TypeScript passes; remote ref verified at `237d43c84b2dea3ec554a4021714048dc72512ab`. Gap task projection, workflow notifications, authority policy, final lock, immutable report package, and UI interaction test coverage remain future work. |
| P0-D Gate/review/approval enforcement | JTBD-007, JTBD-008, JTBD-027, JTBD-028 | Capture gate reviews now server-enforce decision rationale, pass/conditional checklist gates, conditional-pass conditions, reviewer quorum, and reject-vote blocking for pass decisions; document approval and color-team package-freeze enforcement remain future work. | Strengthened `conductGateReview` with decision gates and no-bid terminal path validation. | Added reviewer vote controls, quorum visibility, required checklist warning, conditional-pass condition warning, and server-aligned disabled state to `GateReviewPanel`. | No migration; uses existing `gateReviews` checklist, reviewer, condition, and decision fields. | Required checklist gates, reviewer quorum, vote capture, decision rationale, conditional action item projection, and no-bid terminal update implemented for capture gates. | `npm test -- --run __tests__/actions/gate-review-workflow.test.ts`; `npx tsc --noEmit`. | Pushed | c3bb0d5 | 4 focused gate workflow tests pass; TypeScript passes; remote ref verified at `c3bb0d5a9ac73c5a9f6a810655b7965b89c642dd`. Still needs authority roles, signed audit events, notifications/escalations, document approval state-machine hardening, color-team package freeze, and UI interaction tests. |
| P0-E Production/render/submission | JTBD-047, JTBD-048, JTBD-049, JTBD-050, JTBD-070 | Submission records now require server-side pre-submission audit readiness, submitted-by identity, at least one opportunity-owned attachment, and receipt/confirmation reference; render jobs, e-signature, correction/reversal, and final package approval remain future work. | Strengthened `createSubmission` with audit gate, attachment ownership check, trimmed receipt identity, and SHA-256 artifact snapshot hashes. | Updated `SubmissionForm` to require confirmation/receipt before calling the server action. | No migration; stores `artifactHash` and `lockedAt` inside existing submission attachment JSON. | Submission package gate, receipt capture, opportunity-owned attachment validation, artifact snapshot hash, and submitted opportunity status update implemented for pilot workflow. | `npm test -- --run __tests__/actions/submission-workflow.test.ts`; `npx tsc --noEmit`. | Pushed | 2ce998d | 3 focused submission workflow tests pass; TypeScript passes; remote ref verified at `2ce998d4fac695b6e9ecb5b8ba4e5f24f5a6b313`. Still needs render job lifecycle, artifact storage receipts, final approval/signature workflow, immutable audit events, correction path, and UI interaction tests. |
| P0-F Exception queue and operations | JTBD-064, JTBD-065, JTBD-072, JTBD-074 | Failed RFP parse jobs and failed/partial scraper runs now have a normalized operational exception read model; durable incident ownership, SLA, escalation, and broad remediation remain future work. | Added `listOperationalExceptions` and `remediateOperationalException` to normalize RFP parse and scraper failures and route RFP parse retry/reject/manual-extraction through the existing parse workflow. | Not implemented in this slice; operator UI remains future work. | No migration; derives queue entries from existing RFP parse and scraper run tables. | Unified failed-job subject shape, severity, owner hints, action hints, and RFP remediation handoff implemented for pilot workflow. | `npm test -- --run __tests__/actions/operational-exceptions.test.ts`; `npx tsc --noEmit`. | Pushed | 5a914bc | 4 focused operational exception tests pass; TypeScript passes; remote ref verified at `5a914bc60bb68d9032437260fb29c7ac6b3f00c1`. Still needs persistent exception entity, assignment/escalation, SLA timers, audit trail, scraper rerun/disable remediation action, migration rollback workflow, and operator queue UI. |
| P0-G Cross-cutting workflow runtime, SLA, audit, dashboard, and portal visibility | JTBD-051, JTBD-052, JTBD-057, JTBD-058, JTBD-060, JTBD-063, JTBD-064, JTBD-065, JTBD-075 | A durable workflow runtime now exists for cross-domain workflow instances, assignments, SLA breach/escalation state, audit events, notification queue records, dashboard reads, portal-visible workflow reads, and operational exception materialization. Domain pilot workflows emit runtime records on a best-effort basis so business transitions are not rolled back if the runtime sidecar is unavailable. | Added `workflow-runtime` server actions for transition recording, task projection, SLA evaluation, dashboard aggregation, portal visibility, and authority checks; wired requirements, RFP parse, compliance entries, gate reviews, submissions, and operational exceptions into the runtime sidecar. | Added workflow dashboard, portal, SLA evaluation, and exception-sync API routes. | Added `workflow_instances`, `workflow_runtime_tasks`, `workflow_audit_events`, and `workflow_notifications` via `0016_workflow_runtime.sql`. | Persistent workflow instances, assignment metadata, SLA breach/escalation fields, authority policy metadata, portal visibility metadata, audit events, notification queue rows, condition/gap/manual-extraction task projections, and dashboard summaries are implemented. | `npm test -- --run __tests__/actions/workflow-runtime.test.ts __tests__/actions/requirements-workflow.test.ts __tests__/actions/rfp-parse-workflow.test.ts __tests__/actions/compliance-entry-workflow.test.ts __tests__/actions/gate-review-workflow.test.ts __tests__/actions/submission-workflow.test.ts __tests__/actions/operational-exceptions.test.ts`; `npx tsc --noEmit`. | Committed | 1121497 | 29 focused workflow tests pass; TypeScript passes; implementation committed at `1121497`. Remaining gaps are deployment migration validation, full UI screens for operations dashboard/portal, strict auth middleware on new workflow APIs, worker scheduling for SLA evaluation/exception sync, and complete reversal/compensation semantics. |
| P0-H Runtime hardening, governance, and broad-template coverage | JTBD-029, JTBD-030, JTBD-036, JTBD-037, JTBD-043, JTBD-044, JTBD-051, JTBD-052, JTBD-053, JTBD-055, JTBD-057, JTBD-058, JTBD-061, JTBD-062, JTBD-063, JTBD-067, JTBD-069, JTBD-071, JTBD-073, JTBD-074, JTBD-075 | The runtime is hardened for deployed validation, route-level authorization, worker execution, Stalwart email delivery, universal reversal actions, admin template publication, and published template coverage for evidence, pricing, imports, partner portal, AI governance, reporting, and mobile/offline. | Added strict workflow API actor enforcement with scheduler-secret support, resource permission checks, reversal/transition route, notification dispatch route, template draft/simulate/publish routes, worker script, migration validation/apply scripts, and default workflow template catalogue/seed script. | Added `/workflows`, `/workflows/operations`, `/workflows/portal`, and `/workflows/templates` screens plus sidebar navigation. | Added `workflow_templates` via `0017_workflow_template_governance.sql`; applied `0016` and `0017` to the configured deployed DB and verified table presence. | Stalwart SMTP dispatch for queued email workflow notifications, resilient scheduled worker execution for SLA/exception/notification jobs, cancellation/reopen/resolve audit events, task cancellation on workflow cancel, versioned template simulation/publication/deprecation, and P1/P2/Strategic default templates are implemented. | `npx tsc --noEmit`; `npm test -- --run __tests__/actions/workflow-runtime.test.ts __tests__/workflows/api-auth.test.ts`; `npm run workflow:migrate-db`; `npm run workflow:validate-db`; `WORKFLOW_SLA_LIMIT=0 WORKFLOW_EXCEPTION_LIMIT=0 WORKFLOW_NOTIFICATION_LIMIT=0 npm run workflow:worker`; `npm run workflow:seed-templates`. | Committed | this commit | 13 focused workflow/auth tests pass; TypeScript passes; deployed DB validates workflow tables; template seed published 7 active templates. Runtime is now substantially workflow-capable, but domain-specific end-to-end UI/action coverage for every catalogue JTBD still depends on future vertical workflow integration beyond templates. |
| P0-I Vertical domain integration and compensation handlers | JTBD-006, JTBD-008, JTBD-011, JTBD-012, JTBD-013, JTBD-015, JTBD-020, JTBD-027, JTBD-028, JTBD-029, JTBD-030, JTBD-036, JTBD-037, JTBD-043, JTBD-044, JTBD-047, JTBD-048, JTBD-049, JTBD-050, JTBD-053, JTBD-057, JTBD-058, JTBD-061, JTBD-062, JTBD-075 | Active workflow templates can now start real domain workflow instances, enforce legal template transitions, expose start/reversal UI actions, and apply domain-specific compensation to all currently emitted or seeded subject types. | Added `workflow-domain` server actions, `/api/v1/workflows/start`, routed workflow transition API through template-aware domain transitions and compensation, expanded durable-table handlers, and made strategic/template-only subject state models explicit. | Added workflow action panel on `/workflows` for starting template-backed workflows and applying resolve/reopen/cancel actions with reasons; added Playwright browser coverage for start and resolve interactions. | No new migration; uses existing workflow runtime tables plus existing opportunity, RFP, scraper run, compliance, gate review, task, review, approval, submission, evidence, pricing, import, and partner assignment tables. AI governance events, audit report packages, and offline action batches use explicit workflow-managed state stored in `workflow_instances.metadata.domainState`. | Template transitions now create runtime tasks, apply portal/SLA/authority policy, update real domain state, and record `domain_compensation_applied` audit events. Reversal actions update durable domain tables for opportunity decisions, RFP parse documents/jobs, scraper runs, requirements, compliance entries, gate reviews, proposal tasks, proposal reviews, review comments, document approvals, submissions, evidence claims, pricing packages/cost elements, imports, and partner assignments; workflow-managed subjects record named state models and current state metadata instead of falling back to audit-only handling. | `npx tsc --noEmit`; `npm test -- --run __tests__/actions/workflow-domain.test.ts`; `npm test -- --run __tests__/actions/workflow-runtime.test.ts __tests__/workflows/api-auth.test.ts`; `npx playwright test e2e/workflow-action-panel.spec.ts`. | Verified | this commit | 30 focused workflow/domain/auth tests pass, TypeScript passes, and 1 Playwright browser interaction test passes. Remaining gaps are deeper domain-specific side effects where future tables add richer compensation semantics, plus end-to-end coverage for strategic domains beyond the workflow-managed state model. |

## Continuation Notes

- Keep slices vertical and reviewable.
- Stage only files changed for the active Ralph slice.
- Update `docs/platform-jtbd-catalogue.md` only after verified implementation changes status/evidence.
- After each verified slice, commit using the Lore protocol and push if credentials/network permit.
