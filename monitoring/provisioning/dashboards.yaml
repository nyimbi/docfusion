apiVersion: 1

providers:
  - name: DocFusion
    orgId: 1
    folder: DocFusion
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
